# Single-host inventory pointing at the AWS EC2 target.
# IP is hard-coded — if the instance gets a new public IP (stop/start of a
# non-EIP'd instance does that), update here.
[app]
3.79.7.13 ansible_user=ubuntu
